{
  "name": "visora-workspace",
  "private": true,
  "description": "VISORA monorepo root — npm scripts delegate to the Next.js app in visora/",
  "scripts": {
    "postinstall": "npm install --prefix visora",
    "dev": "npm run dev --prefix visora",
    "dev:debug": "npm run dev:debug --prefix visora",
    "build": "npm run build --prefix visora",
    "start": "npm run start --prefix visora",
    "lint": "npm run lint --prefix visora",
    "test:smoke": "npm run test:smoke --prefix visora"
  }
}

{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && (npm ci --prefix visora || npm install --prefix visora)",
  "buildCommand": "npm run build"
}

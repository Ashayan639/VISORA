{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --prefix visora || npm install --prefix visora",
  "buildCommand": "npm ci --prefix visora || npm install --prefix visora && npm run build --prefix visora"
}
